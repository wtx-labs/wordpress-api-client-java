/*
 * WordPress REST API
 * The WordPress REST API is a powerful part of WordPress that lets you read and write various parts of WordPress data such as posts, pages, users, media, categories, tags, and custom content types defined in the installation.
 *
 * The version of the OpenAPI document: v2
 * Contact: labs@wtx.pl
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package pl.wtx.wordpress.api.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import pl.wtx.wordpress.api.client.model.Crop;
import pl.wtx.wordpress.api.client.model.CropArgs;
import pl.wtx.wordpress.api.client.model.Flip;
import pl.wtx.wordpress.api.client.model.Rotation;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import pl.wtx.wordpress.api.client.invoker.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.13.0")
public class MediaIdEditPostRequestModifiersInner extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(MediaIdEditPostRequestModifiersInner.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!MediaIdEditPostRequestModifiersInner.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'MediaIdEditPostRequestModifiersInner' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<Flip> adapterFlip = gson.getDelegateAdapter(this, TypeToken.get(Flip.class));
            final TypeAdapter<Rotation> adapterRotation = gson.getDelegateAdapter(this, TypeToken.get(Rotation.class));
            final TypeAdapter<Crop> adapterCrop = gson.getDelegateAdapter(this, TypeToken.get(Crop.class));

            return (TypeAdapter<T>) new TypeAdapter<MediaIdEditPostRequestModifiersInner>() {
                @Override
                public void write(JsonWriter out, MediaIdEditPostRequestModifiersInner value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `Flip`
                    if (value.getActualInstance() instanceof Flip) {
                        JsonElement element = adapterFlip.toJsonTree((Flip)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Rotation`
                    if (value.getActualInstance() instanceof Rotation) {
                        JsonElement element = adapterRotation.toJsonTree((Rotation)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Crop`
                    if (value.getActualInstance() instanceof Crop) {
                        JsonElement element = adapterCrop.toJsonTree((Crop)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: Crop, Flip, Rotation");
                }

                @Override
                public MediaIdEditPostRequestModifiersInner read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize Flip
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Flip.validateJsonElement(jsonElement);
                        actualAdapter = adapterFlip;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Flip'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Flip failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Flip'", e);
                    }
                    // deserialize Rotation
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Rotation.validateJsonElement(jsonElement);
                        actualAdapter = adapterRotation;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Rotation'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Rotation failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Rotation'", e);
                    }
                    // deserialize Crop
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Crop.validateJsonElement(jsonElement);
                        actualAdapter = adapterCrop;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Crop'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Crop failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Crop'", e);
                    }

                    if (match == 1) {
                        MediaIdEditPostRequestModifiersInner ret = new MediaIdEditPostRequestModifiersInner();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for MediaIdEditPostRequestModifiersInner: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public MediaIdEditPostRequestModifiersInner() {
        super("oneOf", Boolean.FALSE);
    }

    public MediaIdEditPostRequestModifiersInner(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Flip", Flip.class);
        schemas.put("Rotation", Rotation.class);
        schemas.put("Crop", Crop.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return MediaIdEditPostRequestModifiersInner.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * Crop, Flip, Rotation
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof Flip) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Rotation) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Crop) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Crop, Flip, Rotation");
    }

    /**
     * Get the actual instance, which can be the following:
     * Crop, Flip, Rotation
     *
     * @return The actual instance (Crop, Flip, Rotation)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Flip`. If the actual instance is not `Flip`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Flip`
     * @throws ClassCastException if the instance is not `Flip`
     */
    public Flip getFlip() throws ClassCastException {
        return (Flip)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Rotation`. If the actual instance is not `Rotation`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Rotation`
     * @throws ClassCastException if the instance is not `Rotation`
     */
    public Rotation getRotation() throws ClassCastException {
        return (Rotation)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Crop`. If the actual instance is not `Crop`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Crop`
     * @throws ClassCastException if the instance is not `Crop`
     */
    public Crop getCrop() throws ClassCastException {
        return (Crop)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to MediaIdEditPostRequestModifiersInner
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with Flip
        try {
            Flip.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Flip failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Rotation
        try {
            Rotation.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Rotation failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Crop
        try {
            Crop.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Crop failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for MediaIdEditPostRequestModifiersInner with oneOf schemas: Crop, Flip, Rotation. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of MediaIdEditPostRequestModifiersInner given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of MediaIdEditPostRequestModifiersInner
     * @throws IOException if the JSON string is invalid with respect to MediaIdEditPostRequestModifiersInner
     */
    public static MediaIdEditPostRequestModifiersInner fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, MediaIdEditPostRequestModifiersInner.class);
    }

    /**
     * Convert an instance of MediaIdEditPostRequestModifiersInner to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

